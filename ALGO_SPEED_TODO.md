## 五子棋 AI 算法级性能优化 TODO（以提速为首要目标）

### 目标
- 在不改变棋力评估细则的前提下，优先通过算法剪枝与排序，显著降低分支因子与重复搜索，达到“更快落子”的效果。

### 实施顺序（强烈建议按序）
1) 威胁空间候选与分级排序（落点优先级与限宽）
   - 仅生成/优先扩展强威胁位置：己方成五/挡对手成五 > 活四/挡活四 > 冲四/挡冲四 > 双活三/四三杀 > 单活三/多冲三 > 活二。
   - 采用分桶限宽：高层桶基本全保留，低层桶限制 Top-K（例如 2/2/4/4/8/8，总数控制在 12-28）。
   - 若存在“立即胜/立即防”的候选，优先返回该类候选（必要时直接提前剪枝）。

2) 迭代加深 + PVS（Principal Variation Search）+ 期望窗（Aspiration Window）
   - 迭代加深提供稳定主变，强力提升排序质量；
   - PVS：首子全窗，随后子用窄窗(alpha, alpha+1) 探测，失败再全窗重搜；
   - 期望窗围绕上次分数设定（例如 ±1000，按评分量级调整）。

3) 置换表（TT）+ Zobrist 哈希
   - 存储 {hash, depth, value, flag(LOWER/UPPER/EXACT), best_move}；
   - 命中且深度足够时直接返回；否则据界限剪枝；
   - 优先扩展 TT 的 best_move，以强化排序。

4) 杀手启发 + 历史启发
   - 杀手：每层记录 2 个导致 beta-cut 的非吃子手，排序优先；
   - 历史：全局表 `history[row][col]`，fail-high 累加分，排序时作为次级关键因子。

5) 空着裁剪（Null Move）+ LMR（延迟着法减深）+ Futility（无作为剪枝）
   - 空着：深度≥R+1（R=2/3），且无“立即威胁”（如活四/双活三）时尝试；PV/近终局禁用；
   - LMR：非 PV 节点、排序靠后、深度≥3 时按 (depth, move_index) 计算减深 r；fail-high 回搜全深；
   - Futility：叶前一层若 eval + 上限 < alpha，跳过低价值安静手。

6) 威胁静态搜索（Quiescence/Threat-only）
   - 叶子不直接评估，仅沿成五/挡五/活四/冲四/活三等战术手扩展，直到“安静”。

7) 威胁链解算（VCF/VCT，选做）
   - 单独的小搜索用于验证连续威胁是否成立，可在关键节点调用。

---

### 对 `generate_sorted_moves2` 的针对性优化（就地可做）
- 分桶与早停：
  - 在评分前先执行“强制手侦测”：若某候选在己方视角形成 `FIVE/OPEN_FOUR`，直接将其放入最高桶；若对手视角形成 `FIVE/OPEN_FOUR` 的可防点，只保留这些防守点优先返回。
  - 按优先级分桶排序并限宽（Top-K），避免尾部大量低价值扩展。

- 候选集精炼：
  - 维持“邻域 1 步”为主；在寂静局面可兼容少量“邻域 2 步”的高分点（由打分门槛控制）；
  - 开局（空棋盘）直接返回中心或小十字区域，以免无效全盘扫描。

- 排序信号融合：
  - 目前总分 `own_score + opp_score` 可调整为防守加权：`own_score + λ * opp_score`，当探测到对手存在 `OPEN_FOUR/FOUR_THREE` 等强威胁时，提升 λ（例如 1.5-2.0）；
  - 加入“与上一步距离/当前子群密度”的轻量 tie-breaker，微调等分局面排序稳定性。

- 早期剪枝钩子：
  - 若最高桶非空并且 alpha-beta 窗口很紧，可直接限制只扩展最高桶；
  - 结合未来的 TT/PV：将 TT best_move 与 PV 首着插到列表最前。

- 轻量化快速预估 + 精算两段式：
  - 先用廉价特征（邻居密度、开口数近似等）对全部候选进行粗排，取前 N 做 `regex` 精算；
  - 该策略在节点候选很多时更省时（N 典型值 16-24）。

备注：以上均保持接口不变；如需要更强的上下文（例如按深度动态限宽），可将 `depth/ply/is_pv` 以可选参数传入。

---

### 工程实现 TODO（按需分批，非本轮优先）
- 评估增量化：仅更新经过落子的 4 条线窗口；
- 位运算替代正则：位板/掩码匹配常见棋型；
- 评估/候选缓存：兄弟节点复用（配合 `undo_move` 更新）；
- 统计/剖析：在 `performance_analysis.py` 增加节点数、NPS、TT 命中率、平均分支因子、层级候选分布等指标输出；
- 关键路径 Cython/Numba 加速（在算法收敛后再做）。

---

### 验收标准与度量
- 基准局面固定快照（`board_snapshots/`），以固定时间预算（例如 1s）比较：
  - 最大到达深度↑、搜索节点数↓、NPS↑；
  - 剪枝率、平均分支因子显著下降；
  - 一致性测试 `test_ai_consistency.py` 在同一预算/深度下结果稳定。

---

### 推荐起步顺序（本周即可完成）
1. 在 `generate_sorted_moves2` 内加入“强制手侦测 + 分桶限宽 + 防守权重 λ”三件套（无接口改动）。
2. 外层接入迭代加深 + PVS + 期望窗（保留旧接口，新增 `search(time_limit|max_depth)`）。
3. 引入 Zobrist + 置换表，并在排序中优先 TT best_move。
4. 增加杀手/历史启发，强化排序稳定性。
5. 视效果接入 Null Move/LMR/Futility，最后补威胁静态搜索。


